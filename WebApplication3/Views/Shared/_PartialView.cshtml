@model PersonVM

<div class="row">
    <div class="col">@Model.Id</div>
    <div class="col">@Model.Name</div>
    <div class="col">@Model.City</div>
    <div class="col">@Model.PhoneNumber</div>
    <div class="col">@Model.Register</div>
    <div class="col"><a asp-controller="People" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger"> Delete</a></div>
</div>